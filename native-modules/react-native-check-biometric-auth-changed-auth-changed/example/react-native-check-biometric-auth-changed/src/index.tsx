import ReactNativeCheckBiometricAuthChanged from './NativeReactNativeCheckBiometricAuthChanged';

export function multiply(a: number, b: number): number {
  return ReactNativeCheckBiometricAuthChanged.multiply(a, b);
}
