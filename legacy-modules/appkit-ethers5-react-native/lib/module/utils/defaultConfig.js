export function defaultConfig(options) {
  const {
    metadata,
    extraConnectors
  } = options;
  let providers = {
    metadata,
    extraConnectors
  };
  return providers;
}
//# sourceMappingURL=defaultConfig.js.map