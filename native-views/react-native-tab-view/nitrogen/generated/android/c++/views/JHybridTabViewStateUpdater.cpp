///
/// JHybridTabViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#include "JHybridTabViewStateUpdater.hpp"
#include "views/HybridTabViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::tabview::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridTabViewState>;

void JHybridTabViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridTabViewSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridTabViewSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridTabViewState& data = concreteState->getData();
  const std::optional<HybridTabViewProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridTabViewState's data doesn't contain any props!");
  }
  const HybridTabViewProps& props = maybeProps.value();
  if (props.items.isDirty) {
    view->setItems(props.items.value);
    // TODO: Set isDirty = false
  }
  if (props.selectedPage.isDirty) {
    view->setSelectedPage(props.selectedPage.value);
    // TODO: Set isDirty = false
  }
  if (props.icons.isDirty) {
    view->setIcons(props.icons.value);
    // TODO: Set isDirty = false
  }
  if (props.labeled.isDirty) {
    view->setLabeled(props.labeled.value);
    // TODO: Set isDirty = false
  }
  if (props.sidebarAdaptable.isDirty) {
    view->setSidebarAdaptable(props.sidebarAdaptable.value);
    // TODO: Set isDirty = false
  }
  if (props.disablePageAnimations.isDirty) {
    view->setDisablePageAnimations(props.disablePageAnimations.value);
    // TODO: Set isDirty = false
  }
  if (props.hapticFeedbackEnabled.isDirty) {
    view->setHapticFeedbackEnabled(props.hapticFeedbackEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.scrollEdgeAppearance.isDirty) {
    view->setScrollEdgeAppearance(props.scrollEdgeAppearance.value);
    // TODO: Set isDirty = false
  }
  if (props.minimizeBehavior.isDirty) {
    view->setMinimizeBehavior(props.minimizeBehavior.value);
    // TODO: Set isDirty = false
  }
  if (props.tabBarHidden.isDirty) {
    view->setTabBarHidden(props.tabBarHidden.value);
    // TODO: Set isDirty = false
  }
  if (props.translucent.isDirty) {
    view->setTranslucent(props.translucent.value);
    // TODO: Set isDirty = false
  }
  if (props.barTintColor.isDirty) {
    view->setBarTintColor(props.barTintColor.value);
    // TODO: Set isDirty = false
  }
  if (props.activeTintColor.isDirty) {
    view->setActiveTintColor(props.activeTintColor.value);
    // TODO: Set isDirty = false
  }
  if (props.inactiveTintColor.isDirty) {
    view->setInactiveTintColor(props.inactiveTintColor.value);
    // TODO: Set isDirty = false
  }
  if (props.rippleColor.isDirty) {
    view->setRippleColor(props.rippleColor.value);
    // TODO: Set isDirty = false
  }
  if (props.activeIndicatorColor.isDirty) {
    view->setActiveIndicatorColor(props.activeIndicatorColor.value);
    // TODO: Set isDirty = false
  }
  if (props.fontFamily.isDirty) {
    view->setFontFamily(props.fontFamily.value);
    // TODO: Set isDirty = false
  }
  if (props.fontWeight.isDirty) {
    view->setFontWeight(props.fontWeight.value);
    // TODO: Set isDirty = false
  }
  if (props.fontSize.isDirty) {
    view->setFontSize(props.fontSize.value);
    // TODO: Set isDirty = false
  }
  if (props.onPageSelected.isDirty) {
    view->setOnPageSelected(props.onPageSelected.value);
    // TODO: Set isDirty = false
  }
  if (props.onTabLongPress.isDirty) {
    view->setOnTabLongPress(props.onTabLongPress.value);
    // TODO: Set isDirty = false
  }
  if (props.onTabBarMeasured.isDirty) {
    view->setOnTabBarMeasured(props.onTabBarMeasured.value);
    // TODO: Set isDirty = false
  }
  if (props.onNativeLayout.isDirty) {
    view->setOnNativeLayout(props.onNativeLayout.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridTabViewSpec> shared = javaView->cthis()->shared_cast<JHybridTabViewSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::tabview::views
