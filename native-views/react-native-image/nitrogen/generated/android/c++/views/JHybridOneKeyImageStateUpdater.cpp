///
/// JHybridOneKeyImageStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridOneKeyImageStateUpdater.hpp"
#include "views/HybridOneKeyImageComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::onekeyimage::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridOneKeyImageState>;

std::shared_ptr<const HybridOneKeyImageProps> JHybridOneKeyImageStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridOneKeyImageState& data = concreteState->getData();
  const std::shared_ptr<const HybridOneKeyImageProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridOneKeyImageState's data doesn't contain any props!");
  }
  return props;
}

void JHybridOneKeyImageStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridOneKeyImageSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridOneKeyImageSpec> hybridView = javaView->getJHybridOneKeyImageSpec();
  std::shared_ptr<const HybridOneKeyImageProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridOneKeyImageProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->sourceUri.isProvided()
        : !newProps->sourceUri.hasSameValue(oldProps->sourceUri)) {
    hybridView->setSourceUri(newProps->sourceUri.get());
  }
  if (oldProps == nullptr
        ? newProps->sourceHeadersJson.isProvided()
        : !newProps->sourceHeadersJson.hasSameValue(oldProps->sourceHeadersJson)) {
    hybridView->setSourceHeadersJson(newProps->sourceHeadersJson.get());
  }
  if (oldProps == nullptr
        ? newProps->variant.isProvided()
        : !newProps->variant.hasSameValue(oldProps->variant)) {
    hybridView->setVariant(newProps->variant.get());
  }
  if (oldProps == nullptr
        ? newProps->contentFit.isProvided()
        : !newProps->contentFit.hasSameValue(oldProps->contentFit)) {
    hybridView->setContentFit(newProps->contentFit.get());
  }
  if (oldProps == nullptr
        ? newProps->cachePolicy.isProvided()
        : !newProps->cachePolicy.hasSameValue(oldProps->cachePolicy)) {
    hybridView->setCachePolicy(newProps->cachePolicy.get());
  }
  if (oldProps == nullptr
        ? newProps->autoplay.isProvided()
        : !newProps->autoplay.hasSameValue(oldProps->autoplay)) {
    hybridView->setAutoplay(newProps->autoplay.get());
  }
  if (oldProps == nullptr
        ? newProps->recyclingKey.isProvided()
        : !newProps->recyclingKey.hasSameValue(oldProps->recyclingKey)) {
    hybridView->setRecyclingKey(newProps->recyclingKey.get());
  }
  if (oldProps == nullptr
        ? newProps->optimizeTos.isProvided()
        : !newProps->optimizeTos.hasSameValue(oldProps->optimizeTos)) {
    hybridView->setOptimizeTos(newProps->optimizeTos.get());
  }
  if (oldProps == nullptr
        ? newProps->overscan.isProvided()
        : !newProps->overscan.hasSameValue(oldProps->overscan)) {
    hybridView->setOverscan(newProps->overscan.get());
  }
  if (oldProps == nullptr
        ? newProps->loadingStrategy.isProvided()
        : !newProps->loadingStrategy.hasSameValue(oldProps->loadingStrategy)) {
    hybridView->setLoadingStrategy(newProps->loadingStrategy.get());
  }
  if (oldProps == nullptr
        ? newProps->onLoadStart.isProvided()
        : !newProps->onLoadStart.hasSameValue(oldProps->onLoadStart)) {
    hybridView->setOnLoadStart(newProps->onLoadStart.get());
  }
  if (oldProps == nullptr
        ? newProps->onLoad.isProvided()
        : !newProps->onLoad.hasSameValue(oldProps->onLoad)) {
    hybridView->setOnLoad(newProps->onLoad.get());
  }
  if (oldProps == nullptr
        ? newProps->onDisplay.isProvided()
        : !newProps->onDisplay.hasSameValue(oldProps->onDisplay)) {
    hybridView->setOnDisplay(newProps->onDisplay.get());
  }
  if (oldProps == nullptr
        ? newProps->onError.isProvided()
        : !newProps->onError.hasSameValue(oldProps->onError)) {
    hybridView->setOnError(newProps->onError.get());
  }
  if (oldProps == nullptr
        ? newProps->onLoadEnd.isProvided()
        : !newProps->onLoadEnd.hasSameValue(oldProps->onLoadEnd)) {
    hybridView->setOnLoadEnd(newProps->onLoadEnd.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::onekeyimage::views
