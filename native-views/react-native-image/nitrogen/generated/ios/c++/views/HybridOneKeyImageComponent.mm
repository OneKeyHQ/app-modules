///
/// HybridOneKeyImageComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridOneKeyImageComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridOneKeyImageSpecSwift.hpp"
#import "OneKeyImage-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::onekeyimage;
using namespace margelo::nitro::onekeyimage::views;

/**
 * Represents the React Native View holder for the Nitro "OneKeyImage" HybridView.
 */
@interface HybridOneKeyImageComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridOneKeyImageComponent {
  std::shared_ptr<HybridOneKeyImageSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridOneKeyImageComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridOneKeyImageComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridOneKeyImageShadowNode::defaultSharedProps();
    std::shared_ptr<HybridOneKeyImageSpec> hybridView = OneKeyImage::OneKeyImageAutolinking::createOneKeyImage();
    _hybridView = std::dynamic_pointer_cast<HybridOneKeyImageSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  OneKeyImage::HybridOneKeyImageSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  OneKeyImage::HybridOneKeyImageSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridOneKeyImageProps>(props);
  const auto* oldViewProps = static_cast<const HybridOneKeyImageProps*>(oldProps.get());
  OneKeyImage::HybridOneKeyImageSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // sourceUri: optional
    if (oldViewProps == nullptr
          ? newViewProps.sourceUri.isProvided()
          : !newViewProps.sourceUri.hasSameValue(oldViewProps->sourceUri)) {
      swiftPart.setSourceUri(newViewProps.sourceUri.get());
    }
    // sourceHeadersJson: optional
    if (oldViewProps == nullptr
          ? newViewProps.sourceHeadersJson.isProvided()
          : !newViewProps.sourceHeadersJson.hasSameValue(oldViewProps->sourceHeadersJson)) {
      swiftPart.setSourceHeadersJson(newViewProps.sourceHeadersJson.get());
    }
    // variant: optional
    if (oldViewProps == nullptr
          ? newViewProps.variant.isProvided()
          : !newViewProps.variant.hasSameValue(oldViewProps->variant)) {
      swiftPart.setVariant(newViewProps.variant.get());
    }
    // contentFit: optional
    if (oldViewProps == nullptr
          ? newViewProps.contentFit.isProvided()
          : !newViewProps.contentFit.hasSameValue(oldViewProps->contentFit)) {
      swiftPart.setContentFit(newViewProps.contentFit.get());
    }
    // cachePolicy: optional
    if (oldViewProps == nullptr
          ? newViewProps.cachePolicy.isProvided()
          : !newViewProps.cachePolicy.hasSameValue(oldViewProps->cachePolicy)) {
      swiftPart.setCachePolicy(newViewProps.cachePolicy.get());
    }
    // autoplay: optional
    if (oldViewProps == nullptr
          ? newViewProps.autoplay.isProvided()
          : !newViewProps.autoplay.hasSameValue(oldViewProps->autoplay)) {
      swiftPart.setAutoplay(newViewProps.autoplay.get());
    }
    // recyclingKey: optional
    if (oldViewProps == nullptr
          ? newViewProps.recyclingKey.isProvided()
          : !newViewProps.recyclingKey.hasSameValue(oldViewProps->recyclingKey)) {
      swiftPart.setRecyclingKey(newViewProps.recyclingKey.get());
    }
    // optimizeTos: optional
    if (oldViewProps == nullptr
          ? newViewProps.optimizeTos.isProvided()
          : !newViewProps.optimizeTos.hasSameValue(oldViewProps->optimizeTos)) {
      swiftPart.setOptimizeTos(newViewProps.optimizeTos.get());
    }
    // overscan: optional
    if (oldViewProps == nullptr
          ? newViewProps.overscan.isProvided()
          : !newViewProps.overscan.hasSameValue(oldViewProps->overscan)) {
      swiftPart.setOverscan(newViewProps.overscan.get());
    }
    // loadingStrategy: optional
    if (oldViewProps == nullptr
          ? newViewProps.loadingStrategy.isProvided()
          : !newViewProps.loadingStrategy.hasSameValue(oldViewProps->loadingStrategy)) {
      swiftPart.setLoadingStrategy(newViewProps.loadingStrategy.get());
    }
    // onLoadStart: optional
    if (oldViewProps == nullptr
          ? newViewProps.onLoadStart.isProvided()
          : !newViewProps.onLoadStart.hasSameValue(oldViewProps->onLoadStart)) {
      swiftPart.setOnLoadStart(newViewProps.onLoadStart.get());
    }
    // onLoad: optional
    if (oldViewProps == nullptr
          ? newViewProps.onLoad.isProvided()
          : !newViewProps.onLoad.hasSameValue(oldViewProps->onLoad)) {
      swiftPart.setOnLoad(newViewProps.onLoad.get());
    }
    // onDisplay: optional
    if (oldViewProps == nullptr
          ? newViewProps.onDisplay.isProvided()
          : !newViewProps.onDisplay.hasSameValue(oldViewProps->onDisplay)) {
      swiftPart.setOnDisplay(newViewProps.onDisplay.get());
    }
    // onError: optional
    if (oldViewProps == nullptr
          ? newViewProps.onError.isProvided()
          : !newViewProps.onError.hasSameValue(oldViewProps->onError)) {
      swiftPart.setOnError(newViewProps.onError.get());
    }
    // onLoadEnd: optional
    if (oldViewProps == nullptr
          ? newViewProps.onLoadEnd.isProvided()
          : !newViewProps.onLoadEnd.hasSameValue(oldViewProps->onLoadEnd)) {
      swiftPart.setOnLoadEnd(newViewProps.onLoadEnd.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return OneKeyImage::OneKeyImageAutolinking::isOneKeyImageRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  OneKeyImage::HybridOneKeyImageSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
